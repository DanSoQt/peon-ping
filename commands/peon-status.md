---
name: peon-status
description: Show peon-ping status (active/paused, current pack, volume)
---

Show the current peon-ping configuration.

Run the following command using the Bash tool:

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/peon.py" --status
```

Report the output to the user.
